# OS
.DS_Store

# Runtime-generated state (MVP invariant: filesystem-first, but not versioned)
indices/

# Runtime state (never commit)

runtime_migrate_full/
runtime_migrate_strict/

### BEGIN runtime-ignore (managed)
# Never commit runtime state / generated / local env
runtime/.venv/
runtime/ui/node_modules/
runtime/ui/dist/
runtime/runs/
runtime/proposals/
runtime/closures/
runtime/indices/

# Never commit runtime audit by default (except explicitly allowed notes)
runtime/audit/**

# Allow audit notes (docs)
!runtime/audit/
!runtime/audit/**/
!runtime/audit/**/MIGRATION_CLOSEOUT_NOTE.md
### END runtime-ignore (managed)

# Python caches
__pycache__/
*.pyc

### BEGIN sandbox-ignore (managed)
# Ignore sandbox scratch by default, but allow tooling scripts
sandbox/
!sandbox/
sandbox/*
!sandbox/tools/
sandbox/tools/*
!sandbox/tools/*.sh
### END sandbox-ignore (managed)

# derived outputs
runtime/manifests/chapter_manifest.json

# local/demo tooling
runtime/scripts/run_full_demo.py

# avoid split-authority docs
documentation/system_overview/

# generated exports
runtime/exports/
